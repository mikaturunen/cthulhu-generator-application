import { Kernel } from "inversify";
// import Authentication from "../authentication";

const kernel = new Kernel();
// kernel.bind<IAuthentication>("IAuthentication").to(Ninja);

export default kernel;
